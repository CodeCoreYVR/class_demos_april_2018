// You must the function `console.log()` to output
// text to the terminal. Otherwise when running
// a script, nothing will show.
console.log("Hello, World!");
console.log(`Here's my first script!`);
// This code's value will not show when run as a
// script, because it's not inside a console.log.
1 + 1;
